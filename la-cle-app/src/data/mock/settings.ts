import type { Settings } from "@/types";

export const mockSettings: Settings = {
  siteName: "Institut La Clé",
  supportEmail: "support@lacleformation.fr",
  examPassingScore: 100,
  maxExamAttempts: 3,
  sessionRegistrationDeadlineDays: 7,
  maintenanceMode: false,
};
